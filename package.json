{
  "name": "coffee",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "lint": "exec biome check",
    "lint:fix": "exec biome check --write",
    "lint:staged": "exec biome check --staged --write --no-errors-on-unmatched",
    "prepare": "husky"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "astro": "6.0.0-beta.9",
    "tailwindcss": "^4.2.1",
    "tinacms": "^3.5.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.3",
    "@tinacms/cli": "^2.1.6",
    "@types/node": "^25.3.0",
    "husky": "^9.1.7"
  }
}
